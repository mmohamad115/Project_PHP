<?php

$naam = "";
$leeftijd = 0;
$isGetrouwd = false;
$kinderen = array();
if(empty($naam)){
    echo "<br>";
    var_dump (empty($naam));
}
if(empty($leeftijd)){
    echo "<br>";
    var_dump (empty($leeftijd));
}
if(empty($isGetrouwd)){
    echo "<br>";
    var_dump (empty($isGetrouwd));
}
if(empty($isGetrouwd)){
    echo "<br>";
    var_dump (empty($kinderen));
}
